document.addEventListener('DOMContentLoaded', () => {
    // --- Text setup is done in HTML/SVG ---
    // --- Cake click interactivity is now handled by the <a> link ---

    // const cakeImage = document.getElementById('cake-image'); // No longer needed for click
    // const sparkleEffect = document.getElementById('sparkle-effect'); // No longer needed for click

    // --- Initial Setup ---
    // No specific setup needed here for now
    console.log("Birthday page loaded."); // Optional: confirm script runs

});